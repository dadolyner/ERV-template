import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './pages/App'

ReactDOM.createRoot(document.getElementById('ERV-Template')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)